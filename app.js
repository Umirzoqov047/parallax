let options = {
  strings: [
    "Hech kim sendan kuchli emas,",
    "Faqat ular oldinroq boshlagan",
    "Stiv Jobs",
  ],
  typeSpeed: 60,
  startDelay: 500,
  backSpeed: 40,
  backDelay: 1000,
  loop: true,
  loopCount: Infinity,
};
let typed = new Typed(".element", options);
